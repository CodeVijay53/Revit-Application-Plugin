<Window x:Class="FinalApplication.Views.CreateMultipleSheets"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Create Multiple Sheets" Height="250" Width="400" ResizeMode="NoResize">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Text="Select a Title Block Template:" Margin="0,0,0,10" />
        <ComboBox x:Name="TitleBlockComboBox" Margin="0,0,0,130" Grid.Row="3" />

        <TextBlock Text="Enter Number of Sheets:" Margin="0,36,0,99" Grid.Row="3" />
        <TextBox x:Name="NumberOfSheetsTextBox" Margin="0,64,0,66" Grid.Row="3" />

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="OkButton" Content="OK" Width="80" Margin="5" />
            <Button x:Name="CancelButton" Content="Cancel" Width="80" Margin="5" />
        </StackPanel>
    </Grid>
</Window>
