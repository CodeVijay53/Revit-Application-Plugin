<Window x:Class="FinalApplication.Views.FamilyParameters"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Select Family File" Height="174" Width="450" ResizeMode="NoResize">
    <Grid Margin="10,10,10,23">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <TextBlock Text="Select a Family File:" VerticalAlignment="Center" />
        <TextBox x:Name="FilePathTextBox" Grid.Row="1" Width="300" HorizontalAlignment="Left" Margin="0,5,0,27" />
        <Button x:Name="BrowseButton" Grid.Row="1" Content="Browse..." Width="80" HorizontalAlignment="Right" Margin="0,5,0,27" />
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="OkButton" Content="OK" Width="80" />
            <Button x:Name="CancelButton" Content="Cancel" Width="80" />
        </StackPanel>
    </Grid>
</Window>
